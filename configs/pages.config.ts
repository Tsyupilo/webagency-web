import type { MenuLang } from "./site.config";

type PagesHeadings = {
	allPages: MenuLang;
};

export const pagesHeadings: PagesHeadings = {
	allPages: {
		en: "All Pages",
		ru: "Все страницы",
	},
};
