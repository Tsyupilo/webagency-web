import type { MenuLang } from "./site.config";

type PostsHeadings = {
	allPosts: MenuLang;
};

export const postsHeadings: PostsHeadings = {
	allPosts: {
		en: "All Posts",
		ru: "Все посты",
	},
};
